\فصل{نتایج آزمایش‌ها}

در این فصل، نتیجهٔ سه مسیر اصلی معرفی‌شده در فصل پیش بررسی می‌شود: ریزتنظیم ویسپر-کوچک با داده‌های معمولی فارسی، ریزتنظیم همان مدل با ترکیب داده‌های معمولی و تخریب‌شده، و مدل همجوشی دوجریانه‌ای که دو نمای اصلی و بهسازی‌شده را هم‌زمان پردازش می‌کند. هدف فقط مرتب‌کردن مدل‌ها بر اساس یک عدد نهایی نیست؛ نتایج باید روشن کنند که آیا شبیه‌سازی تخریب‌های مخابراتی واقعاً به کانال‌های واقعی تعمیم می‌یابد، آیا پیچیدگی مدل همجوشی سودی فراتر از آموزش چندشرایطی دارد، و این سود یا زیان در مجموعه‌های آزمون مختلف چگونه توزیع شده است.

بر همین اساس، تحلیل فصل حول سه پرسش انجام می‌شود: نخست، افزودن داده‌های تخریب‌شده چه مقدار از خطای مدل تک‌جریانه می‌کاهد؟ دوم، آیا این بهبود در گفتار واقعی تلفنی و \lr{VoIP} نیز دیده می‌شود یا فقط به داده‌های عمومی محدود است؟ سوم، آیا نمای بهسازی‌شده در مدل دوجریانه اطلاعات مکملی فراهم می‌کند که خط مبنای چندشرایطی به آن دسترسی نداشته باشد؟

\قسمت{پروتکل ارزیابی}

دو معیار اصلی، نرخ خطای واژه\پاورقی{Word Error Rate (WER)} و نرخ خطای نویسه\پاورقی{Character Error Rate (CER)} هستند. اگر تعداد جانشینی‌ها، حذف‌ها و درج‌ها را به‌ترتیب با $S$، $D$ و $I$ و تعداد واحدهای متن مرجع را با $N$ نشان دهیم، نرخ خطا از رابطهٔ زیر به دست می‌آید:

\begin{equation}
\mathrm{ER}=\frac{S+D+I}{N}.
\end{equation}

در محاسبهٔ WER، واحد شمارش واژه و در محاسبهٔ CER، نویسه است. بنابراین مقدار کمتر در هر دو معیار بهتر است، اما این دو معیار جنبه‌های یکسانی از خطا را اندازه نمی‌گیرند. WER به درست‌بودن کل واژه حساس است، درحالی‌که CER شدت تفاوت نوشتاری درون واژه را نیز نشان می‌دهد. به همین دلیل، هم‌جهت‌نبودن این دو معیار می‌تواند نشانهٔ تفاوت در الگوی خطا باشد و نباید یکی را جایگزین کامل دیگری دانست.

همهٔ مدل‌ها با رمزگشایی حریصانه و یک زنجیرهٔ یکسان برای نرمال‌سازی متن ارزیابی شدند. پنج مجموعهٔ آزمون جدول~\رجوع{جدول:داده-آزمون} هم گفتار عمومی فارسی و هم گفتار واقعی مخابراتی را پوشش می‌دهند. AGFarsdat نرمال‌شده از دید هدف پژوهش مهم‌ترین مجموعه است، زیرا نمونه‌های تلفنی و \lr{VoIP} واقعی دارد و خروجی شبیه‌ساز تخریب این پژوهش نیست. در نتیجه، بهبود روی این مجموعه شاهدی برای انتقال آموخته‌های حاصل از تخریب مصنوعی به یک کانال واقعی است. سایر مجموعه‌ها برای بررسی این موضوع به کار می‌روند که افزایش مقاومت مخابراتی به قیمت افت گسترده در گفتار عمومی تمام نشده باشد.

دو اجرای تک‌جریانه دقیقاً روی $21{,}848$ نمونه امتیازدهی شدند. در هر دو اجرا، $6$ نمونه به‌دلیل عبور طول برچسب از سقف $448$ توکن کنار گذاشته شد. ارزیاب مدل همجوشی $21{,}854$ نمونه را ثبت کرده است: یک نمونهٔ بیش‌تر از کامن‌وویس و پنج نمونهٔ بیش‌تر از پیکرهٔ گفتار فارسی. بنابراین، مقایسهٔ ویسپر معمولی و ویسپر چندشرایطی کاملاً جفت‌شده است، اما مقایسهٔ عدد تجمیعی مدل همجوشی با آن دو مدل، اختلافی شش‌نمونه‌ای دارد. این اختلاف فقط $0.027\%$ از کل نمونه‌ها است، ولی برای رعایت دقت روش‌شناختی، تفاوت‌های کوچک مدل همجوشی به‌عنوان برتری قطعی یا معنادار آماری تفسیر نمی‌شوند.

تنظیمات دو اجرای تک‌جریانه، به‌جز ترکیب داده، یکسان بود: بذر تصادفی $1337$، یک دورهٔ آموزش، نرخ یادگیری $10^{-5}$، اندازهٔ دستهٔ $4$ و انباشت گرادیان در $8$ گام. در اجرای چندشرایطی، دو مجموعهٔ تخریب‌شدهٔ کامن‌وویس به داده‌های معمولی و گونه‌های بلند افزوده شدند. مدل همجوشی با برنامهٔ سه‌مرحله‌ای فصل پیش آموزش یافت: حداکثر $20{,}000$ گام گرم‌کردن بهساز، $30{,}000$ گام آموزش بهساز و همجوشی با ویسپر ثابت، و $120{,}000$ گام آموزش مشترک انتهابه‌انتها. ازاین‌رو، مقایسهٔ دو مدل تک‌جریانه اثر دادهٔ تخریب‌شده را با کنترل مناسب‌تری جدا می‌کند؛ مقایسهٔ مدل همجوشی افزون بر تغییر معماری، تفاوت برنامه و مدت آموزش را نیز در بر دارد.

\قسمت{نتایج تجمیعی}

جدول~\رجوع{جدول:نتایج-کلی} نتیجهٔ تجمیعی را روی همهٔ مجموعه‌های آزمون نشان می‌دهد. ستون آخر تغییر مطلق WER نسبت به ویسپر معمولی را گزارش می‌کند؛ علامت منفی به معنای کاهش خطا است. مقدار تجمیعی میانگین سادهٔ پنج سطر جدول تفکیکی نیست و از کل پیش‌بینی‌ها و مراجع ارزیابی محاسبه شده است. در نتیجه، مجموعه‌های بزرگ‌تر و جمله‌های دارای واژه‌های بیش‌تر سهم بیشتری در این عدد دارند.

\شروع{لوح}[ht]
\تنظیم‌ازوسط
\شرح{نتایج تجمیعی سه روش اصلی روی همهٔ مجموعه‌های آزمون}
\resizebox{\textwidth}{!}{%
\شروع{جدول}{|c|c|c|c|}
\خط‌پر
\سیاه روش & \سیاه WER & \سیاه CER & \سیاه تغییر مطلق WER نسبت به ویسپر معمولی \\
\خط‌پر \خط‌پر
ویسپر-کوچک با دادهٔ معمولی و گونه‌های بلند & \lr{23.27\%} & \lr{10.84\%} & --- \\
ویسپر-کوچک با دادهٔ معمولی، بلند و تخریب‌شده & \مهم{\lr{17.83\%}} & \مهم{\lr{7.93\%}} & \مهم{\lr{-5.44}} واحد درصد \\
همجوشی دوجریانهٔ ویسپر، اجرای نهایی & \lr{18.44\%} & \lr{14.04\%} & \lr{-4.82} واحد درصد \\
\خط‌پر
\پایان{جدول}}
\برچسب{جدول:نتایج-کلی}
\پایان{لوح}

افزودن داده‌های تخریب‌شده، WER را از $23.27\%$ به $17.83\%$ رسانده است. این تغییر برابر با $5.44$ واحد درصد کاهش مطلق و حدود $23.4\%$ کاهش نسبی خطاست. CER نیز از $10.84\%$ به $7.93\%$ کاهش یافته است؛ یعنی $2.91$ واحد درصد کاهش مطلق و حدود $26.8\%$ کاهش نسبی. چون داده‌های ارزیابی و تنظیمات بهینه‌سازی دو اجرا یکسان‌اند، این نتیجه قوی‌ترین شاهد آزمایش حاضر برای اثر آموزش چندشرایطی است: مواجه‌کردن مستقیم بازشناس با نویز، محدودیت پهنای‌باند، مصنوعات کدک و افت بسته، هم در سطح واژه و هم در سطح نویسه خطا را به‌طور محسوسی کم کرده است.

مدل همجوشی نیز نسبت به ویسپر معمولی WER کمتری دارد: کاهش مطلق $4.82$ واحد درصد، معادل حدود $20.7\%$ کاهش نسبی. بااین‌حال، WER آن $0.62$ واحد درصد از مدل چندشرایطی بیشتر است. افزون بر این، CER مدل همجوشی به $14.04\%$ رسیده و از هر دو مدل تک‌جریانه بدتر است. بنابراین، نتیجهٔ تجمیعی دو پیام متفاوت دارد: مدل دوجریانه بخش مهمی از مقاومت مدل چندشرایطی را حفظ کرده، اما پیچیدگی بهسازی و همجوشی هنوز به بهبود کلی نسبت به قوی‌ترین خط مبنا منجر نشده و الگوی خطای نویسه‌ای آن نیز نیازمند بررسی دقیق‌تر است.

\قسمت{نتایج به تفکیک مجموعهٔ آزمون}

عدد تجمیعی برای انتخاب مدل نهایی مفید است، اما تفاوت حوزه‌های صوتی را پنهان می‌کند. جدول~\رجوع{جدول:نتایج-wer}، WER را به تفکیک مجموعه نشان می‌دهد. بهترین مقدار هر سطر پررنگ شده است؛ این پررنگ‌سازی صرفاً کمترین عدد مشاهده‌شده را نشان می‌دهد و به معنای اثبات معناداری آماری اختلاف نیست.

\شروع{لوح}[ht]
\تنظیم‌ازوسط
\شرح{WER به تفکیک مجموعهٔ آزمون؛ مقدار کمتر بهتر است}
\resizebox{\textwidth}{!}{%
\شروع{جدول}{|c|c|c|c|}
\خط‌پر
\سیاه مجموعهٔ آزمون & \سیاه ویسپر معمولی & \سیاه ویسپر چندشرایطی & \سیاه همجوشی دوجریانه \\
\خط‌پر \خط‌پر
AGFarsdat (تلفنی/VoIP واقعی) & \lr{35.63\%} & \lr{25.80\%} & \مهم{\lr{25.38\%}} \\
کامن‌وویس فارسی ۲۵ & \lr{11.52\%} & \مهم{\lr{7.84\%}} & \lr{9.06\%} \\
فلورز فارسی & \lr{21.05\%} & \lr{19.82\%} & \مهم{\lr{19.61\%}} \\
پرشین‌اسپیچ & \lr{34.53\%} & \lr{35.25\%} & \مهم{\lr{31.18\%}} \\
پیکرهٔ گفتار فارسی & \مهم{\lr{30.17\%}} & \lr{30.90\%} & \lr{35.17\%} \\
\خط‌پر
\پایان{جدول}}
\برچسب{جدول:نتایج-wer}
\پایان{لوح}

\زیرقسمت{گفتار واقعی تلفنی و VoIP}

مهم‌ترین بهبود در AGFarsdat دیده می‌شود. آموزش چندشرایطی WER را از $35.63\%$ به $25.80\%$ کاهش داده است؛ یعنی $9.83$ واحد درصد کاهش مطلق و حدود $27.6\%$ کاهش نسبی. چون AGFarsdat در تولید دادهٔ تخریب‌شده یا آموزش مدل نقشی نداشته و کانال‌های واقعی مخابراتی را شامل می‌شود، این نتیجه نشان می‌دهد تنوع مصنوعی آموزش صرفاً حفظ نشده، بلکه دست‌کم تا حدی به شرایط واقعی انتقال یافته است.

مدل همجوشی در همین مجموعه WER برابر $25.38\%$ دارد و کمترین مقدار ثبت‌شده را به دست آورده است. اختلاف آن با مدل چندشرایطی فقط $0.42$ واحد درصد، یا حدود $1.6\%$ نسبت به خطای مدل چندشرایطی، است. بدون بازنمونه‌گیری آماری یا فاصلهٔ اطمینان نمی‌توان این فاصلهٔ کوچک را برتری پایدار مدل همجوشی دانست. تفسیر محتاطانه‌تر آن است که مدل دوجریانه در دامنهٔ اصلی پژوهش عملکرد مدل چندشرایطی را حفظ کرده و اندکی بهبود عددی ایجاد کرده است، اما این بهبود کوچک برای جبران افت آن در برخی مجموعه‌های بزرگ‌تر کافی نیست.

\زیرقسمت{گفتار عمومی و تفاوت حوزه‌ها}

در کامن‌وویس، مدل چندشرایطی بهترین نتیجه را دارد و WER را از $11.52\%$ به $7.84\%$ می‌رساند؛ کاهش مطلق $3.68$ واحد درصد و کاهش نسبی حدود $31.9\%$. بخشی از این سود می‌تواند ناشی از آن باشد که دادهٔ تخریب‌شده از بخش آموزش همین پیکره ساخته شده است، هرچند نمونه‌های آزمون در آموزش حضور نداشته‌اند. مدل همجوشی با WER برابر $9.06\%$ همچنان از ویسپر معمولی بهتر است، اما $1.23$ واحد درصد از مدل چندشرایطی عقب می‌ماند.

در فلورز، فاصلهٔ سه مدل کوچک‌تر است. آموزش چندشرایطی WER را $1.23$ واحد درصد کاهش می‌دهد و مدل همجوشی با $19.61\%$ فقط $0.20$ واحد درصد بهتر از آن است. این نتیجه با AGFarsdat هم‌جهت است، اما اندازهٔ اختلاف بار دیگر برای ادعای برتری قطعی همجوشی کافی نیست.

پرشین‌اسپیچ تنها $24$ نمونهٔ ارزیابی‌شده دارد. مدل همجوشی در این مجموعه WER را نسبت به مدل چندشرایطی $4.07$ واحد درصد کاهش می‌دهد، ولی هر نمونه می‌تواند اثر بزرگی بر نتیجه داشته باشد؛ بنابراین این سطر بیشتر یک مشاهدهٔ مقدماتی است تا شاهدی محکم برای تعمیم‌پذیری. برای نتیجه‌گیری معتبر، به مجموعه‌ای بزرگ‌تر از همان دامنه یا فاصلهٔ اطمینان مبتنی بر بازنمونه‌گیری نیاز است.

در پیکرهٔ گفتار فارسی، روند برعکس است: ویسپر معمولی با WER برابر $30.17\%$ بهترین نتیجه را دارد، آموزش چندشرایطی خطا را اندکی به $30.90\%$ افزایش می‌دهد و مدل همجوشی با $35.17\%$ ضعیف‌ترین نتیجه را ثبت می‌کند. این پسرفت نشان می‌دهد افزودن مقاومت کانالی به‌طور یکنواخت به همهٔ دامنه‌ها سود نمی‌رساند. یکی از توضیح‌های محتمل، تفاوت سبک گفتار، کیفیت ضبط یا توزیع متن این پیکره با داده‌های غالب آموزش است؛ بااین‌حال، داده‌های موجود برای نسبت‌دادن قطعی افت به یکی از این عوامل کافی نیستند.

\قسمت{تحلیل خطای نویسه}

برای آشکارشدن تفاوت الگوی خطا، جدول~\رجوع{جدول:نتایج-cer} مقدار CER هر مجموعه را نیز گزارش می‌کند. مدل چندشرایطی در نتیجهٔ تجمیعی بهترین CER را دارد، اما بهترین مدل در همهٔ مجموعه‌ها یکسان نیست.

\شروع{لوح}[ht]
\تنظیم‌ازوسط
\شرح{CER به تفکیک مجموعهٔ آزمون؛ مقدار کمتر بهتر است}
\resizebox{\textwidth}{!}{%
\شروع{جدول}{|c|c|c|c|}
\خط‌پر
\سیاه مجموعهٔ آزمون & \سیاه ویسپر معمولی & \سیاه ویسپر چندشرایطی & \سیاه همجوشی دوجریانه \\
\خط‌پر \خط‌پر
AGFarsdat (تلفنی/VoIP واقعی) & \lr{19.73\%} & \lr{12.62\%} & \مهم{\lr{12.38\%}} \\
کامن‌وویس فارسی ۲۵ & \lr{4.06\%} & \مهم{\lr{3.22\%}} & \lr{16.08\%} \\
فلورز فارسی & \مهم{\lr{6.13\%}} & \lr{6.36\%} & \lr{6.23\%} \\
پرشین‌اسپیچ & \lr{16.21\%} & \lr{15.62\%} & \مهم{\lr{12.91\%}} \\
پیکرهٔ گفتار فارسی & \lr{15.48\%} & \مهم{\lr{15.22\%}} & \lr{21.07\%} \\
\خط‌پر
\پایان{جدول}}
\برچسب{جدول:نتایج-cer}
\پایان{لوح}

در AGFarsdat، نتیجهٔ CER نتیجهٔ WER را تأیید می‌کند: هر دو روش مقاوم نسبت به ویسپر معمولی بهبود بزرگی دارند و مدل همجوشی اندکی بهتر است. در پرشین‌اسپیچ نیز همجوشی کمترین CER را ثبت می‌کند. در مقابل، CER مدل همجوشی روی کامن‌وویس به $16.08\%$ می‌رسد، با آن‌که WER همان اجرا $9.06\%$ است. همچنین CER آن در پیکرهٔ گفتار فارسی $21.07\%$ است. همین دو مجموعه بخش مهمی از افزایش CER تجمیعی مدل همجوشی را توضیح می‌دهند.

از فایل‌های معیار تجمیعی نمی‌توان تعیین کرد که این واگرایی دقیقاً از چند خروجی بسیار بلند، تکرار نویسه‌ها، خطاهای نرمال‌سازی یا الگوی دیگری ناشی شده است. نتیجهٔ قابل دفاع در وضعیت فعلی این است که WER نسبتاً خوب مدل همجوشی نباید ضعف CER آن را پنهان کند. تحلیل نمونه‌به‌نمونهٔ درج، حذف و جانشینی و بازبینی خروجی‌های پرت، پیش‌نیاز هر ادعای دقیق‌تر دربارهٔ علت این رفتار است.

\قسمت{تحلیل روند توسعهٔ مدل همجوشی}

دو اجرای ذخیره‌شده از مدل همجوشی در دسترس است. نسخهٔ نخست از ویسپر-کوچک عمومی آغاز شد و نسخهٔ نهایی، مطابق پیکربندی آموزش، از نقطهٔ وارسی ویسپر فارسیِ چندشرایطی استفاده کرد. در نسخهٔ نهایی، یک ترنسفورمر دولایه نیز به گلوگاه بهساز افزوده شد، گرم‌کردن بهساز شامل زیان تطبیق ویژگی رمزگذار بود و سقف آموزش مشترک از $50{,}000$ به $120{,}000$ گام افزایش یافت. چون چند تغییر هم‌زمان اعمال شده‌اند، اختلاف دو نسخه را نمی‌توان به یک مؤلفهٔ خاص نسبت داد؛ بااین‌حال، مقایسهٔ آن‌ها روند توسعه را روشن می‌کند.

\شروع{لوح}[ht]
\تنظیم‌ازوسط
\شرح{مقایسهٔ دو اجرای ذخیره‌شدهٔ مدل همجوشی}
\resizebox{\textwidth}{!}{%
\شروع{جدول}{|c|c|c|c|c|}
\خط‌پر
\سیاه اجرا & \سیاه WER & \سیاه CER & \سیاه میانگین وزن نمای بهسازی‌شده & \سیاه میانگین وزن نمای اصلی \\
\خط‌پر \خط‌پر
نسخهٔ نخست & \lr{24.35\%} & \lr{15.46\%} & \lr{0.5154} & \lr{0.4846} \\
نسخهٔ نهایی & \مهم{\lr{18.44\%}} & \مهم{\lr{14.04\%}} & \lr{0.0039} & \lr{0.9961} \\
\خط‌پر
\پایان{جدول}}
\برچسب{جدول:روند-همجوشی}
\پایان{لوح}

WER نسخهٔ نهایی نسبت به نسخهٔ نخست $5.90$ واحد درصد کاهش یافته است. بااین‌حال، رفتار دروازه به‌جای انتخاب متعادل یا وابسته به شرایط، تقریباً به یک جریان فروپاشیده است. میانگین وزن نمای بهسازی‌شده $0.0039$ و میانهٔ آن $0.00393$ است؛ در مقابل، میانگین و میانهٔ وزن نمای اصلی به‌ترتیب $0.9961$ و $0.99607$ هستند. نزدیکی میانگین و میانه نشان می‌دهد این نتیجه صرفاً حاصل چند نمونهٔ پرت نیست و مدل در بخش بزرگی از ارزیابی به نمای اصلی تکیه دارد.

این رفتار توضیح محتملی برای نرسیدن مدل همجوشی به خط مبنای چندشرایطی فراهم می‌کند: مسیر بهسازی‌شده در تصمیم نهایی سهم بسیار کمی دارد و بخش افزودهٔ معماری عملاً اطلاعات مکمل پایداری عرضه نمی‌کند. بااین‌حال، وزن دروازه به‌تنهایی برای اثبات بی‌فایده‌بودن بهساز کافی نیست. توجه متقاطع پیش از دروازه می‌تواند اطلاعات دو جریان را جابه‌جا کند و مقدار میانگین نیز تغییرات موضعی زمان--ویژگی را خلاصه می‌کند. برای جداکردن این اثرها باید همان نقطهٔ وارسی در حالت‌های «فقط نمای اصلی»، «فقط نمای بهسازی‌شده»، «همجوشی بدون توجه متقاطع» و «دروازه با وزن تحمیلی» ارزیابی شود.

\قسمت{تهدیدهای اعتبار و دامنهٔ نتیجه‌گیری}

نتایج این فصل شواهد روشنی به سود آموزش چندشرایطی فراهم می‌کنند، اما دامنهٔ ادعا باید با محدودیت‌های آزمایش سازگار بماند:

\شروع{فقرات}
\فقره هر پیکربندی تک‌جریانه با یک بذر و یک اجرای آموزشی گزارش شده است. در نتیجه، پراکندگی ناشی از مقداردهی اولیه، ترتیب دسته‌ها و انتخاب نقطهٔ وارسی اندازه‌گیری نشده است.
\فقره برای اختلاف مدل‌ها فاصلهٔ اطمینان یا آزمون معناداری آماری محاسبه نشده است. این محدودیت به‌ویژه برای برتری‌های کوچک همجوشی در AGFarsdat و فلورز مهم است.
\فقره مدل همجوشی شش نمونهٔ بیش‌تر از دو مدل تک‌جریانه ارزیابی کرده است. بنابراین مقایسهٔ تجمیعی آن کاملاً جفت‌شده نیست، هرچند اندازهٔ اختلاف نمونه کوچک است.
\فقره WER و CER به تفکیک نمایهٔ تخریب، بازهٔ SNR، کدک، نرخ افت بسته و طول کلیپ گزارش نشده‌اند. در نتیجه، معلوم نیست کدام جزء شبیه‌ساز بیشترین سهم را در مقاومت ایجاد کرده است.
\فقره نتیجهٔ تجمیعی تحت تأثیر مجموعه‌های بزرگ AGFarsdat و کامن‌وویس است و نباید به معنای عملکرد یکنواخت در همهٔ دامنه‌ها تفسیر شود.
\فقره ارزیابی حاضر کیفیت بازشناسی را اندازه می‌گیرد، نه کیفیت ادراکی صوت یا بازنمایی بهسازی‌شده. معیارهایی مانند SI-SDR و PESQ و نیز آزمون شنیداری اجرا نشده‌اند.
\فقره هزینهٔ محاسباتی، حافظهٔ مصرفی و تأخیر استنتاج مدل دوجریانه اندازه‌گیری نشده است. ازاین‌رو، حتی در مجموعه‌هایی که همجوشی اندکی بهتر است، دربارهٔ صرفهٔ استقرار آن نمی‌توان نتیجه‌گیری کرد.
\پایان{فقرات}

\قسمت{جمع‌بندی فصل}

پاسخ پرسش نخست روشن است: افزودن داده‌های تخریب‌شده به آموزش ویسپر، WER تجمیعی را $23.4\%$ و CER را $26.8\%$ به‌صورت نسبی کاهش داده و بهترین نتیجهٔ کلی را ساخته است. پاسخ پرسش دوم نیز مثبت اما محدود است: کاهش نسبی WER در AGFarsdat واقعی حدود $27.6\%$ است و نشان می‌دهد تخریب‌های مصنوعی آموزش قابلیت انتقال به گفتار مخابراتی واقعی را دارند. در عین حال، افت کوچک در پیکرهٔ گفتار فارسی یادآور می‌شود که این سود در همهٔ دامنه‌ها یکسان نیست.

پاسخ پرسش سوم در وضعیت فعلی منفی است. مدل همجوشی نسبت به ویسپر معمولی پیشرفت مهمی دارد و در AGFarsdat و فلورز کمترین WER را ثبت می‌کند، اما در نتیجهٔ تجمیعی به مدل چندشرایطی نمی‌رسد، CER ضعیف‌تری دارد و دروازهٔ آن تقریباً همیشه نمای اصلی را انتخاب می‌کند. بنابراین، نتیجهٔ اصلی این فصل برتری یک راهکار ساده‌تر و قابل اتکاتر است: در داده‌ها و تنظیمات حاضر، آموزش مستقیم ویسپر با ترکیب گفتار معمولی و تخریب‌شده مؤثرترین روش برای افزایش مقاومت بازشناسی گفتار فارسی در شرایط مخابراتی است.
